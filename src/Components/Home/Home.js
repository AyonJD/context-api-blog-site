import Blog from "../Blog/Blog";
import "./Home.css";

const Home = () => {

  return (
    // <div className='blogs-container'>
    //   {blogs.map((blog, index) => (
    //     <Blog key={index} blog={blog} />
    //   ))}
    // </div>
    <h1>Home page</h1>
  );
};

export default Home;
